import { createStyles, makeStyles } from '@material-ui/core/styles';

const portletStyles = () =>
  createStyles({
    card: {
      width: '100%',
    },
  });

export default makeStyles(portletStyles);
